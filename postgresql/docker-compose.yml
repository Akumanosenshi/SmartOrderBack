version: '3.8'
name: smartorder-postgres
services:
  postgres-db:
    image: postgres:15
    container_name: postgres_smartorder
    environment:
      POSTGRES_DB: smartOrder
      POSTGRES_USER: smartorder_user
      POSTGRES_PASSWORD: V82g!dxgZ!tR9#qm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - backend-net

volumes:
  postgres_data:

networks:
  backend-net:
